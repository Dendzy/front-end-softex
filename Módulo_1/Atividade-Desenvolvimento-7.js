var numeroTabuada, i;

numeroTabuada = window.prompt('Qual número você deseja fazer a tabuada?');
for (i = 1; i <= 10; i++) {
  window.alert(numeroTabuada * i);
}

/* Esse foi o código que consegui fazer em JavaScript, por não saber programar ficou limitado a simplismente aparecer na tela do usuário
Mas obvimente pensei em outras formas de ficar mais fácil pro usuário.
Por exemplo de guardar todos os resultados em uma lista e depois imprimir para o usuário, ou até mesmo no código de repetição você
fazer um print dizendo que esse numero foi multiplicado por esse e que dá o resultado, mas no momento não sei programar isso. */

/* Em forma de leigo ficaria

Variável numeroTabuada */
x = 0
n = 1

/* Peço ao usuario pra colocar o numero que ele quer, que ficará na variável numeroTabuada

Na repetição, enquanto x for menor que 10 fazer */

resultado = numeroTabuada * n
n = n + 1 (incrementa)

print("Seu numero " + numeroTabuada + " multiplicado por " + n + " é igual a " + resultado);
 
